project('indexsort', 'cpp',
  license : 'MIT',
  license_files : ['LICENSE'],
  default_options : ['warning_level=3', 'cpp_std=c++17'])

message('Use of -Dcpp_debugstl=true is recommended in debug builds.')

inc = include_directories('include')

subdir('test')
subdir('docs')
